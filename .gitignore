# THE ANSWER KEY. This repo publishes to jmumimf.github.io, so anything
# committed here is public. Keep your own backup outside the repo.
answers.json

# Event databases — real submissions, not source
*.db
*.db-wal
*.db-shm

# Exports
estimathon-*.json
estimathon-*.csv
results.csv

__pycache__/
*.pyc
